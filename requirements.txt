# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0     # PostgreSQL driver (optional, SQLite used by default)

# Document processing
python-docx==1.1.2           # DOCX generation + extraction
PyMuPDF>=1.24.0              # PDF text extraction (optional)
Pillow>=10.0.0               # Image processing (optional)
pytesseract>=0.3.10          # OCR for images (optional)

# LLM / AI
groq==0.11.0                 # Groq client for LLM access
openai>=1.0.0                # OpenAI client (for extraction agent)

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Utils
python-multipart>=0.0.9      # File upload support
